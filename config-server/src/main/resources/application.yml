server:
  port: 8084
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/noetipo/microservicios_2023_1.git
          search-paths: config-data
          username: noetipo
          password: Tipo47259697
          default-label: main
  security:
    user:
      name: root
      password: s3cr3t
